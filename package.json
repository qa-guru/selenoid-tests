{
  "private": true,
  "name": "selenoid-tests-allure",
  "description": "Pinned Allure 3 CLI for selenoid-tests CI; notifications use the GitHub Action",
  "engines": {
    "node": ">=26"
  },
  "dependencies": {
    "allure": "3.14.3"
  }
}

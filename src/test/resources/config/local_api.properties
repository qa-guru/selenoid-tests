# local — api (@Layer(api) @Tag(api) — Rest Assured HTTP smoke)
# ./gradlew testApi -DskipHealthCheck=true

hubUrl=http://127.0.0.1:4444/
uiUrl=http://127.0.0.1:8080/
apiBaseUrl=

logToConsole=true
selenideLogToConsole=false
